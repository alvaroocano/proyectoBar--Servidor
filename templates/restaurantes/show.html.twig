{% extends 'base.html.twig' %}

{% block title %}Restaurantes{% endblock %}

{% block body %}
    <h1>Restaurantes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurante.id }}</td>
            </tr>
            <tr>
                <th>Localidad</th>
                <td>{{ restaurante.localidad }}</td>
            </tr>
            <tr>
                <th>Horario</th>
                <td>{{ restaurante.horario }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ restaurante.telefono }}</td>
            </tr>
            <tr>
                <th>Aforo</th>
                <td>{{ restaurante.aforo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurantes_index') }}">back to list</a>

    <a href="{{ path('app_restaurantes_edit', {'id': restaurante.id}) }}">edit</a>

    {{ include('restaurantes/_delete_form.html.twig') }}
{% endblock %}
